/// Интерфейс для всего, что может получать урон.
/// Это удобно: стрелы/удары/ловушки не зависят от конкретного класса врага.
abstract class Damageable {
  /// Получить урон.
  void takeDamage(int value);

  /// Мёртв ли объект (чтобы не стрелять в трупы).
  bool get isDead;
}
